# Отчет по проекту LifeRoadmap 0.5

## 1. Суть и идея проекта
**LifeRoadmap** — это современный менеджер иерархических дорожных карт, реализованный как PWA (Progressive Web App). 

**Основная концепция:**
*   **Древовидная структура**: Любая задача может стать подкартой. Это позволяет декомпозировать глобальные цели на неограниченное количество уровней.
*   **Приватность и безопасность**: Сквозное шифрование данных (E2EE) гарантирует, что даже провайдер облачных услуг не имеет доступа к содержимому ваших задач.
*   **Автономность**: Приложение спроектировано по принципу "Offline-First" — оно полностью функционально без интернета.

## 2. Технологический стек
Проект построен на актуальном стеке технологий, обеспечивающем высокую скорость работы и масштабируемость:
*   **Frontend**: React 18 + TypeScript + Vite.
*   **Стилизация**: TailwindCSS (UI) + Framer Motion (анимации).
*   **Хранение данных**: IndexedDB (локально) + Firebase Firestore (облако).
*   **Безопасность**: Web Crypto API (AES-GCM 256-bit).
*   **Синхронизация**: Собственная реализация на базе Change Logs и стратегии LWW (Last-Write-Wins).
*   **PWA**: Интеграция через `vite-plugin-pwa` для установки на устройства и оффлайн-работы.

## 3. Архитектура и реализация
### Хранение данных
Несмотря на иерархическую суть, в базе данных (и локальной, и облачной) узлы хранятся "плоским" списком. Связи устанавливаются через `parentId`. Дерево реконструируется на лету в памяти приложения, что позволяет:
1. Быстро синхронизировать отдельные изменения.
2. Легко перемещать целые ветки между родителями.
3. Избегать глубокой вложенности в документах базы данных.

### Модель безопасности
Приложение предлагает уникальный выбор между удобством и параноидальной безопасностью:
*   **Standard Mode**: Ключ шифрования хранится в защищенном хранилище Firebase. Это позволяет серверу (Telegram-боту) расшифровывать названия для отправки осмысленных уведомлений.
*   **Enhanced Mode**: Ключ хранится на Google Drive пользователя. Сервер не имеет доступа к ключу, и уведомления приходят в формате "🔒 Зашифрованная задача".

### Синхронизация
Синхронизация работает в фоновом режиме с использованием дебаунса (500мс), чтобы минимизировать количество запросов к API. Реализована система разрешения конфликтов, которая позволяет пользователю сравнить локальную и облачную версии данных в случае их расхождения.

## 4. Статистика проекта
Количественные показатели исходного кода (на момент обновления данного файла):
*   **Общее количество строк кода**: ~22,650
*   **Количество исходных файлов**: 57 (включая компоненты, хуки и утилиты)
*   **Тестовое покрытие**: Наличие юнит-тестов на базе Vitest.

## 5. Инновационные и функциональные решения
### UI/UX решения
*   **Mobile Action Drum**: Специальный элемент управления для мобильных устройств. Вертикальный прокручивающийся список действий с магнитным эффектом, решающий проблему перегруженности интерфейса кнопками.
*   **Drag-and-Drop**: Полноценная поддержка изменения порядка задач как на десктопе, так и на мобильных устройствах.
*   **Визуальные эффекты**:
    *   **Burning Effect**: Эффектная анимация "сгорания" карточки при окончательном удалении выполненной задачи.
    *   **Seasonal UI**: Автоматическое включение декораций (снег, гирлянды) в зависимости от времени года.
    *   **Confetti**: Праздничный эффект при завершении всей ветки задач.

### Функциональные особенности
*   **Telegram-бот**: Полноценная интеграция для напоминаний. Бот учитывает часовой пояс пользователя и статус шифрования.
*   **Умный расчет прогресса**: Алгоритм учитывает только "листовые" узлы (конечные задачи), но позволяет мгновенно завершить всю ветку, отметив родителя.
*   **Импорт/Экспорт**: Возможность экспортировать любую ветку в JSON и импортировать её обратно, что удобно для обмена шаблонами дорожных карт.
*   **Multi-Tab Sync**: Использование `BroadcastChannel` гарантирует, что изменения, сделанные в одной вкладке браузера, мгновенно отобразятся во всех остальных.

## 6. Инфраструктура
*   **CI/CD**: Использование GitHub Actions для автоматической сборки и деплоя на GitHub Pages.
*   **Background Workers**: Скрипты на TypeScript для автоматической обработки уведомлений и обслуживания базы данных (очистка удаленных узлов, миграции).

---
**Итог**: LifeRoadmap — это зрелый, технически сложный проект, сочетающий в себе лучшие практики современной веб-разработки: типизацию, безопасность, оффлайн-работу и продуманный UX.

Где каждая строчка была написана нейросетью, кроме той, которую вы сейчас читаете. Разработка велась при помощи следующего технологического AI стека:

**Основная среда разработки**: Cursor с Pro подпиской

**Дополнительная среда для разработки идей** — Perplexity с Pro подпиской (по акции)

**Модели**:

**Composer 1** - Основная модель до выхода gemini 3 flash

**Gemini 3 flash** - Основная модель благодаря соотношению цена/качество

**GPT 5.2 Codex** - Основная модель для решения сложных задач (так как gemini иногда не справлялся)