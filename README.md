# LifeRoadmap

PWA-менеджер вложенных роадмапов (узел=мапа)

## Технологии

- React + TypeScript
- Vite
- TailwindCSS
- IndexedDB (idb)
- vite-plugin-pwa
- Vitest
- react-icons (иконки)
- react-beautiful-dnd (drag-and-drop)

## Установка

```bash
npm install
```

## Разработка

```bash
npm run dev
```

## Сборка

```bash
npm run build
```

## Деплой на GitHub Pages

### Автоматический деплой (рекомендуется)

1. Включите GitHub Pages в настройках репозитория:
   - Settings → Pages → Source: "GitHub Actions"

2. Workflow автоматически запустится при пуше в ветку `main` или `master`.

3. Если имя репозитория отличается от `NewLifeRoadmap0.5`, обновите в `.github/workflows/deploy.yml`:
   ```yaml
   GITHUB_PAGES_BASE: /your-repo-name/
   ```

### Ручной деплой

```bash
# Сборка с base path для GitHub Pages
npm run build:gh-pages

# Затем загрузите содержимое папки dist в ветку gh-pages
# или используйте GitHub Actions
```

**Важно:** Если репозиторий называется `username.github.io`, используйте `base: '/'` вместо `/repo-name/`.

## Предпросмотр

```bash
npm run preview
```

## Тесты

```bash
npm test
```

## Структура проекта

- `src/types.ts` - типы данных
- `src/db.ts` - API IndexedDB
- `src/utils.ts` - утилиты (progress, deadlines, breadcrumbs)
- `src/i18n.ts` - локализация (RU)
- `src/hooks/` - React хуки (theme, accent, routing, toast)
- `src/components/` - UI компоненты
- `src/pages/` - страницы приложения

## Особенности

- Полностью оффлайн-first (IndexedDB + PWA)
- Хэш-роутинг (#/node/<id>)
- Темная/светлая тема + выбор акцента (8 цветов)
- Импорт/экспорт JSON поддерева
- Undo при удалении (5 сек)
- Прогресс с учётом правила "родитель готов → потомки готовы"
- Ближайшие дедлайны (только для незавершённых задач) с автоматическим обновлением статуса
- Приоритетные задачи с обводкой акцентом и закреплением сверху
- Drag-and-drop для изменения порядка шагов
- Сортировка по имени и дедлайну (с иконками)
- Компактные карточки с иконками действий и tooltip
- Описание отображается под названием узла и в карточках
- Палитра цветов адаптирована для всех цветов

## Функции

### Приоритетные задачи
- Флажок "Приоритетная задача" в редакторе
- Автоматическое закрепление приоритетных задач сверху списка
- Обводка акцентом для визуального выделения
- Кнопка-иконка для быстрого переключения приоритета

### Сортировка и упорядочивание
- Drag-and-drop: перетягивание за иконку из трёх полосок слева от названия
- Сортировка по имени (иконка FiSortAsc)
- Сортировка по дедлайну (иконка FiCalendar)
- Приоритетные задачи всегда остаются сверху

### Интерфейс
- Компактные карточки шагов (все элементы в один ряд)
- Иконки вместо текстовых кнопок:
  - ✓ Отметить выполненным/невыполненным
  - ⬆ Приоритетная задача
  - ✎ Редактировать
  - 🗑 Удалить
- Tooltip при наведении на иконки
- Описание под названием узла и в карточках (с троеточием)
- Дедлайн отображается только для незавершённых задач
